# PyInstaller version resource.
#
# ⚠️ This is not cosmetic. An executable with NO company, product or version metadata
# scores worse against Defender's ML heuristics - "unsigned, unnamed, unversioned" is
# the shape of the thing it is trained to catch. It is not sufficient on its own (the
# 09/01/2026 detection was on the Inno installer, not the freeze), but it is free and it
# moves the score in the right direction.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 1, 0, 0),
    prodvers=(0, 1, 0, 0),
    mask=0x3f, flags=0x0, OS=0x40004, fileType=0x1, subtype=0x0, date=(0, 0)
  ),
  kids=[
    StringFileInfo([
      StringTable('040904B0', [
        StringStruct('CompanyName',      'WA0O'),
        StringStruct('FileDescription',  'HamDeck Wavelog Pusher'),
        StringStruct('FileVersion',      '0.1.0.0'),
        StringStruct('InternalName',     'hamdeck-pusher'),
        StringStruct('LegalCopyright',   'MIT licensed'),
        StringStruct('OriginalFilename', 'hamdeck-pusher.exe'),
        StringStruct('ProductName',      'HamDeck Wavelog Pusher'),
        StringStruct('ProductVersion',   '0.1.0.0'),
      ])
    ]),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)
